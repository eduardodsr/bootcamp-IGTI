var a = 2;
var b = 3;
var total = a + b;

a = 5;          // variavel 'a' já declarada, não precisa declarar novamente a mesama variavel.
total = a + b;  // variavel 'total' já declarada, não precisa declarar novamente a mesama variavel.

console.log(total);